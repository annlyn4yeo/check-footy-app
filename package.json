{
  "name": "check-footy",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "prisma:generate": "prisma generate --schema packages/db/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema packages/db/prisma/schema.prisma",
    "dev:web": "pnpm --filter @checkfooty/web dev",
    "dev:worker": "pnpm --filter @checkfooty/worker dev"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
